#mqtt

mqtt:
  broker: 192.168.1.10

sensor:
  - platform: mqtt
    name: "Temperature"
    state_topic: "/andur/andur1/data"
    unit_of_measurement: '°C'
    value_template: "{{ value_json.temp }}"
  
  
  
  - platform: mqtt
    name: "Humidity"
    state_topic: "/andur/andur1/data"
    unit_of_measurement: '%'
    value_template: "{{ value_json.hum }}"
